<header>
  <nav>
    <% if user_signed_in? %>
      <%= link_to t('.goals_create'), new_goal_path %> |
      <%= link_to t('.goals_index'), goals_path %> |
      <%= link_to t('.logout'), destroy_user_session_path, data: { turbo_method: :delete } %>
    <% else %>
      <%= link_to t('.account_create'), new_user_registration_path %>
    <% end %>
  </nav>
</header>